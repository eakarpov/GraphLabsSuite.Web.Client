import * as React from 'react';
import {Route, Switch} from "react-router";

const Routes = () => (
    <Switch>
        <Route path="/" component={/* tslint:disable-line */() => null} />
    </Switch>
);

export default Routes;