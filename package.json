{
  "name": "GraphLabs.Web.Client",
  "author": "GraphLabs",
  "description": "Client-side of the GraphLabs web project",
  "version": "0.0.1",
  "dependencies": {

  },
  "devDependencies": {

  },
  "scripts": {
    "startDev": "tsc && set NODE_ENV=development && node build/main.js",
    "start": "tsc && set NODE_ENV=production && node build/main.js",
    "compile": "tsc"
  }
}